1. 산출 파일: [poster.html](~/html-diagram-compare/gpt56-terra/poster.html), 115,123 bytes. 검사기: [verify-poster.mjs](~/html-diagram-compare/gpt56-terra/verify-poster.mjs)

2. 노드 21개, 엣지 13개, 캔버스 2개입니다.

3. `node verify-poster.mjs` 정적 검사 통과:
   - 노드 ID와 인라인 위치·너비
   - 엣지 끝점 유효성
   - em/en dash 0개
   - 제목 교체
   - 엔진 CSS·스크립트 바이트 동일
   - 최소 글자 12px, 본문 15px
   - 두 캔버스의 `data-size` 범위 정합

4. 화살표는 실선·파선·점선, end·both·none, 곡률, 투명도, 흐름 애니메이션, 상하좌우 앵커, 비율 앵커, 좌표 끝점, 라벨을 모두 사용했습니다. 노드는 배경 카드, 일반 카드, 원형, 투명 캡션, 반투명 유리형, 그림자 카드, 리치 텍스트, 목록·인용, 표, 크기·정렬, 편집 흐름, 단축키, 이력, 저장 유형을 포함합니다.

5. 어려웠던 점은 엔진 블록을 전혀 바꾸지 않은 채 두 캔버스에 모든 기능을 밀도 있게 배치하고, 텍스트 하한과 좌표 범위를 동시에 만족시키는 일이었습니다. 요청대로 브라우저 검증은 수행하지 않았습니다.